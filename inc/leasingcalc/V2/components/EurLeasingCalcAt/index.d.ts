import { Options } from '../../defaultOptions.ts';

export interface ComponentProps {
    options: Options;
}
declare const EurLeasingCalcAt: ({ options }: ComponentProps) => import("react/jsx-runtime").JSX.Element;
export default EurLeasingCalcAt;
