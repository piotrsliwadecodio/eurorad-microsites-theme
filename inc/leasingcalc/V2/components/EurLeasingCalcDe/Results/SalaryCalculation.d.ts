import { Results } from '../Result';

interface SalaryCalculationProps {
    results?: Results;
    extended?: boolean;
}
declare const SalaryCalculation: ({ results, extended }: SalaryCalculationProps) => import("react/jsx-runtime").JSX.Element;
export default SalaryCalculation;
